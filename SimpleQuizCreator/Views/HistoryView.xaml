<UserControl x:Class="SimpleQuizCreator.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">
    <DockPanel >
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Height="30">
            <Button Width="100" Margin="2" Content="Refresh" Command="{Binding RefreshCommand}"></Button>
        </StackPanel>
        <DataGrid ItemsSource="{Binding HistoryResult}"></DataGrid>
    </DockPanel>
</UserControl>
