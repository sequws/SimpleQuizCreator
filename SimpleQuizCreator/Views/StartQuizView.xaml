<UserControl x:Class="SimpleQuizCreator.Views.StartQuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600" Background="AliceBlue">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top">
            <Label Content="Select quiz"></Label>
            <ComboBox ItemsSource="{Binding ListOfQuizzes}" SelectedItem="{Binding SelectedQuiz}" ></ComboBox>
        </StackPanel>
        <Button DockPanel.Dock="Bottom"  Width="120" Content="Start Quiz" Command="{Binding OpenQuizWindow}"></Button>
        <Grid></Grid>        
    </DockPanel>
</UserControl>
