<Window x:Class="SimpleQuizCreator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:resx="clr-namespace:SimpleQuizCreator.Properties"
        prism:ViewModelLocator.AutoWireViewModel="True" Height="350" Width="525" 
        WindowStartupLocation="CenterScreen" Closed="Window_Closed" Icon="/SimpleQuizCreator;component/QuizIcon.ico" 
        Title="{Binding Title}"
         MinWidth="500" MinHeight="300"
        >
    <DockPanel Background="AliceBlue">
        <Menu Height="20" DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="Save settings"></MenuItem>
                <MenuItem  Header="Close"></MenuItem>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About SimpleQuizCreator"></MenuItem>
            </MenuItem>
        </Menu>
        <StackPanel DockPanel.Dock="Left" Width="140" Background="Bisque">
            <Button Content="HelloView" Margin="2" Height="30"  Command="{Binding NavigateCommand}" CommandParameter="HelloView"></Button>
            <Button Content="QuizListView" Margin="2" Height="30" Command="{Binding NavigateCommand}" CommandParameter="QuizListView"></Button>
            <Button Content="StartQuizView" FontWeight="Bold" Margin="2" Height="30" Command="{Binding NavigateCommand}" CommandParameter="StartQuizView"></Button>
            <Button Content="History"  Margin="2" Height="30" Command="{Binding NavigateCommand}" CommandParameter="HistoryView"></Button>
        </StackPanel>
        <ContentControl prism:RegionManager.RegionName="ContentRegion" />
    </DockPanel>
</Window>
